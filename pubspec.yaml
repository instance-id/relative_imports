name: relative_imports
description: Converts project package imports to relative.
homepage: https://instance.id
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  path: ^1.7.0
  args: ^1.6.0
  codemod: ^0.2.0
  logger: ^0.9.1
  pubspec_parse: ^0.1.5
  dartx: ^0.4.2

dev_dependencies:
  pedantic: ^1.9.0
